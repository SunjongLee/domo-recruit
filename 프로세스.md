# 실행 프로세스 — DOMO 채용 관리 툴

> 작업할 때마다 이 파일을 업데이트합니다. 현재 어디까지 왔고 다음에 무엇을 해야 하는지 항상 여기서 확인합니다.

---

## 현재 상태

**2026-05-16** — 디자인 리뷰용 2개 화면 구현 완료. 서버 실행 후 Robin 디자인 확인 대기 중.

---

## 다음 할 일

1. `npm run dev` 실행해서 브라우저에서 두 화면 확인
2. Robin 디자인 톤앤매너 피드백 반영
3. 피드백 완료 후 면접관 피드백 폼 구현

---

## 전체 단계

### 0단계: 프로젝트 초기 세팅
- [x] PRD.md 작성
- [x] Next.js 15 프로젝트 생성 (`domo-recruit-next/`)
- [x] Supabase CLI 설치 (dev dependency)
- [x] Git 초기화 및 첫 커밋
- [ ] GitHub 레포 생성 및 연결 (Robin이 직접: `gh repo create`)
- [x] Tailwind에 DOMO 브랜드 컬러 등록 (`globals.css`)
- [x] mock 데이터 파일 작성 (`lib/mockData.ts`)

### 1단계: 디자인 리뷰용 화면 2개
- [x] 지원자 포털 UI (`/candidate/[uuid]`) — localStorage mock
- [x] 관리자 대시보드 UI (`/admin`) — localStorage mock
- [ ] **↳ Robin 디자인 확인 체크포인트** ← 지금 여기

### 2단계: 전체 프론트엔드 완성 (localStorage 기반 실제 흐름)
- [ ] 면접관 피드백 폼 (`/feedback/[uuid]/[round]`)
- [ ] 모든 상태 전환 인터랙션 구현
- [ ] 지원자 메시지 → 관리자 확인 흐름
- [ ] 면접 슬롯 선택 → 확정 흐름
- [ ] **↳ 전체 서비스 흐름 점검 체크포인트**

### 3단계: 백엔드 연결 (Supabase + Google)
- [ ] Supabase 프로젝트 생성 + 스키마 마이그레이션
- [ ] localStorage → Supabase API 교체
- [ ] Google OAuth 관리자 인증 연결
- [ ] Google Calendar API 면접 슬롯/초대 연동
- [ ] Vercel 배포 + 환경변수 설정

---

## 결정 사항 기록

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-05-16 | Next.js 15 + Supabase 조합 선택 | 확장성, 보안, 개발 생산성 |
| 2026-05-16 | 프론트엔드 먼저, 백엔드 나중 | 디자인 톤앤매너 먼저 확인하고 싶음 |
| 2026-05-16 | localStorage로 mock 데이터 | 백엔드 없이 전체 흐름 점검 가능 |
| 2026-05-16 | 기존 코드 재활용 안 함 | Node.js → Next.js 전환이라 구조 자체가 다름 |
| 2026-05-16 | Supabase CLI를 dev dependency로 설치 | 글로벌 설치 권한 없음 |

---

## 막힌 것들 / 주의 사항

- GitHub 레포 생성은 Robin이 직접 해야 함 (터미널에서 `gh repo create` 또는 github.com에서 생성 후 `git remote add origin [url]` + `git push -u origin main`)

---

## 프로젝트 구조

```
/home/robin/0516/domo-recruit-next/
├── PRD.md                              ← 기획서
├── 프로세스.md                          ← 이 파일
├── lib/
│   └── mockData.ts                     ← localStorage mock 데이터 (지원자 3명)
├── app/
│   ├── globals.css                     ← DOMO 브랜드 컬러 정의
│   ├── layout.tsx
│   ├── page.tsx
│   ├── candidate/[uuid]/page.tsx       ✅ 지원자 포털
│   ├── admin/page.tsx                  ✅ 관리자 대시보드
│   └── feedback/[uuid]/[round]/        ⬜ 면접관 피드백 (미구현)
└── components/                         ← 공통 컴포넌트 (추후)
```

---

## 개발 서버 실행 방법

```bash
cd /home/robin/0516/domo-recruit-next
npm run dev
```

- 지원자 포털: http://localhost:3000/candidate/candidate-001
- 관리자 대시보드: http://localhost:3000/admin
